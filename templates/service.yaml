apiVersion: v1
kind: Service
metadata:
  name: {{ include "mi-nginx.fullname" . }}
  labels:
    {{- include "mi-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mi-nginx.selectorLabels" . | nindent 4 }}
